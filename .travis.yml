language: python
python:
  - "2.7"
  - "3.6"

install:
  - pip install PyScaffold==2.5   # For numdifftools
  - pip install -r requirements.txt
  - python setup.py install

script:
  - pytest -v -n 2 --durations=0
